spring:
  application:
    name: gateway
  config:
    import: "optional:configserver:http://localhost:8888"

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health, info
