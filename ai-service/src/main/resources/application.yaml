spring:
  application:
    name: ai-service
  config:
    import: "optional:configserver:http://localhost:8888"

server:
  port: 8085

management:
  endpoints:
    web:
      exposure:
        include: health